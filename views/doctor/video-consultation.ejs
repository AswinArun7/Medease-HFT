<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Video Consultation - Medease</title>
    
    <link rel="shortcut icon" type="image/x-icon" href="/assets/img/favicon.png">
    <link rel="stylesheet" type="text/css" href="/assets/css/bootstrap.min.css">
    <link rel="stylesheet" href="/assets/plugins/fontawesome/css/all.min.css">
    
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            overflow: hidden;
        }
        
        .video-container {
            position: relative;
            width: 100vw;
            height: 100vh;
            display: flex;
            flex-direction: column;
        }
        
        .video-header {
            background: rgba(0, 0, 0, 0.7);
            padding: 15px 30px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            color: white;
            z-index: 100;
        }
        
        .patient-info {
            display: flex;
            align-items: center;
            gap: 15px;
        }
        
        .patient-avatar {
            width: 45px;
            height: 45px;
            border-radius: 50%;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 20px;
        }
        
        .video-frame {
            flex: 1;
            width: 100%;
            border: none;
            background: #000;
        }
        
        .controls-bar {
            background: rgba(0, 0, 0, 0.8);
            padding: 20px;
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 20px;
            position: absolute;
            bottom: 0;
            width: 100%;
            z-index: 100;
        }
        
        .control-btn {
            width: 60px;
            height: 60px;
            border-radius: 50%;
            border: none;
            background: rgba(255, 255, 255, 0.2);
            color: white;
            font-size: 24px;
            cursor: pointer;
            transition: all 0.3s;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        
        .control-btn:hover {
            background: rgba(255, 255, 255, 0.3);
            transform: scale(1.1);
        }
        
        .control-btn.end-call {
            background: #e74c3c;
            width: 80px;
        }
        
        .control-btn.end-call:hover {
            background: #c0392b;
        }
        
        .room-info {
            position: absolute;
            top: 80px;
            right: 20px;
            background: rgba(0, 0, 0, 0.7);
            padding: 15px 20px;
            border-radius: 10px;
            color: white;
            font-size: 12px;
            z-index: 101;
        }
        
        .room-code {
            font-weight: bold;
            font-size: 14px;
            color: #4CAF50;
            margin-top: 5px;
        }
    </style>
</head>
<body>
    <div class="video-container">
        <!-- Header -->
        <div class="video-header">
            <div class="patient-info">
                <div class="patient-avatar">
                    <i class="fas fa-user"></i>
                </div>
                <div>
                    <h5 style="margin: 0;"><%= patientName %></h5>
                    <small style="opacity: 0.8;"><%= patientEmail %></small>
                </div>
            </div>
            <div>
                <h6 style="margin: 0;">Dr. <%= doctor.firstname %> <%= doctor.lastname %></h6>
                <small style="opacity: 0.8;">Consultation Session</small>
            </div>
        </div>
        
        <!-- Room Info -->
        <div class="room-info">
            <div>Room ID:</div>
            <div class="room-code"><%= roomId %></div>
            <div style="margin-top: 10px; font-size: 11px; opacity: 0.7;">
                Share this code with patient to join
            </div>
        </div>
        
        <!-- Video Frame (vod.ninj embed) -->
        <iframe 
            id="videoFrame"
            class="video-frame"
            allow="camera; microphone; fullscreen; display-capture"
            allowfullscreen
        ></iframe>
        
        <!-- Controls -->
        <div class="controls-bar">
            <button class="control-btn" id="toggleVideo" title="Toggle Video">
                <i class="fas fa-video"></i>
            </button>
            <button class="control-btn" id="toggleAudio" title="Toggle Audio">
                <i class="fas fa-microphone"></i>
            </button>
            <button class="control-btn" id="shareScreen" title="Share Screen">
                <i class="fas fa-desktop"></i>
            </button>
            <button class="control-btn" id="createReport" title="Create Report" onclick="window.location.href='/doctor/report-generation?appointmentId=<%= appointmentId %>'">
                <i class="fas fa-file-medical"></i>
            </button>
            <button class="control-btn end-call" id="endCall" title="End Call">
                <i class="fas fa-phone-slash"></i>
            </button>
            <button class="control-btn" id="toggleChat" title="Toggle Chat">
                <i class="fas fa-comment"></i>
            </button>
        </div>
    </div>

    <script>
        // Initialize video call
        const roomId = '<%= roomId %>';
        const doctorName = 'Dr. <%= doctor.firstname %> <%= doctor.lastname %>';
        const videoFrame = document.getElementById('videoFrame');
        
        // Load vdo.ninja video call
        const videoCallUrl = `https://vdo.ninja/?room=${roomId}&push=${encodeURIComponent(roomId)}&label=${encodeURIComponent(doctorName)}`;
        videoFrame.src = videoCallUrl;
        
        // Control buttons
        document.getElementById('endCall').addEventListener('click', function() {
            if (confirm('Are you sure you want to end this consultation?')) {
                window.location.href = '/doctor/profile';
            }
        });
        
        // These controls will be handled by vod.ninj interface
        // But we can send messages to the iframe if needed
        document.getElementById('toggleVideo').addEventListener('click', function() {
            this.classList.toggle('active');
            if (this.querySelector('i').classList.contains('fa-video')) {
                this.querySelector('i').classList.replace('fa-video', 'fa-video-slash');
            } else {
                this.querySelector('i').classList.replace('fa-video-slash', 'fa-video');
            }
        });
        
        document.getElementById('toggleAudio').addEventListener('click', function() {
            this.classList.toggle('active');
            if (this.querySelector('i').classList.contains('fa-microphone')) {
                this.querySelector('i').classList.replace('fa-microphone', 'fa-microphone-slash');
            } else {
                this.querySelector('i').classList.replace('fa-microphone-slash', 'fa-microphone');
            }
        });
        
        // Keyboard shortcuts
        document.addEventListener('keydown', function(e) {
            // Escape to go back
            if (e.key === 'Escape') {
                if (confirm('End consultation?')) {
                    window.location.href = '/doctor/profile';
                }
            }
        });
        
        // Copy room code functionality
        document.querySelector('.room-code').addEventListener('click', function() {
            const code = this.textContent;
            navigator.clipboard.writeText(code).then(() => {
                const originalText = this.textContent;
                this.textContent = 'Copied!';
                setTimeout(() => {
                    this.textContent = originalText;
                }, 2000);
            });
        });
    </script>
</body>
</html>
